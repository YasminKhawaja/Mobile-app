import { View, Text, Image, StyleSheet, TouchableOpacity, Touchable } from "react-native";
import { useNavigation } from "@react-navigation/native";

const ProductCard = {{title, description, price, image, onPress}} => {
    const navigation = useNavigation();
    return {

        <TouchableOpacity style={StyleSheet.button} onPress={(onPress) => naivigation.navigate("Detail")})>
    }
}